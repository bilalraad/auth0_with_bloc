import 'package:flutter/material.dart';

class BreedTab extends StatelessWidget {
  const BreedTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blueGrey,
    );
  }
}
